language: go

go:
  - "1.14"

script:
  - test -z $(gofmt -l .)
  - go test
  - go build

after_success:
  - test -n "$TRAVIS_TAG"

deploy:
  provider: script
  skip_cleanup: true
  script: curl -sL https://git.io/goreleaser | bash
  on:
    tags: true
